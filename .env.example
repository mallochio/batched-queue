# Optional: use a cheaper model than the session driver for objective planning.
# When unset, the Pi session driver model is used for both driver and executor.
# Format: provider/model-id
# BATCH_QUEUE_EXECUTOR=openai/gpt-5.4-nano

# Max sequential actions per batch (default 5)
# BATCH_QUEUE_MAX_ACTIONS=5
